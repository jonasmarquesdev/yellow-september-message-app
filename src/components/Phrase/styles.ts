import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  titleContainer: {
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "#fff",
    borderRadius: 16,
    margin: 20,
    padding: 10,
  },
  title: {
    fontSize: 18,
    fontWeight: "700",
    fontStyle: "italic",
    textAlign: 'center'
  },
});